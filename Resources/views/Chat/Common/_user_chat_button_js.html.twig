<script>
    newPrivateButton("{{ user.username }}", "{% if not chat_url %}{{ path('ant_chat_chat_show', {user: user.username } )}}{% else %}{{chat_url}}?user={{ user.username  | url_encode}}{% if app.user is not null %}&nick={{  app.user.username}}{% endif %}{% endif %}", "{{ path('ant_user_user_message', {'id': user.id})  }}");
</script>
{# Finish Code to button go to chat #}